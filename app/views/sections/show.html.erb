<p id="notice"><%= notice %></p>

<h2><%= @section.name %></h2>
<% @section.videos.each do |video| %>
	<%= link_to "#{video.name}", video_path %>
<% end %>
<%= link_to 'Edit', edit_section_path(@section) %> |
<%= link_to 'Back', course_path %>
